# Ethereum node RPC endpoints (needs eth + txpool API namespaces)
ETH_HTTP_URL=http://127.0.0.1:8545
ETH_WS_URL=ws://127.0.0.1:8546

# Server
LISTEN_ADDR=:8080
DB_PATH=./explorer.db

# Data retention
RETENTION_HOURS=48
MAX_POOL=10000
